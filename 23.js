let name = "anil";

function getName() {
  console.log(name);
}
getName();

/*
1> let has block level scope. In this name has been declared in global scope so 
it can be used anywhere inside code file.
*/
