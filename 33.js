let data1 = 'true';
// convert data to boolean false value
console.log(!data1);

let data2 = "true";
// convert data to boolean true value
console.log(!!data2);

// to convert string into boolean false just add NOT operator (!) once in front of it

// to convert string into boolean false just add NOT operator (!) twice in front of it 
