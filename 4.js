let z = [1, 2, 3, 4]
let a = { name: "anil" }
console.log(...z);

/**
 1> inside console statement we have destructured the array so each array element 
 become individual element in itself so it prints 1 2 3 4 like a string
 */