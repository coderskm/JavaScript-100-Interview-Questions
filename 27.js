const channel = "Subscribe";
const age = 21;
console.log(isNaN(channel))
console.log(isNaN(age))

/*
isNaN is used to check whether the given value to it as argument is number or not
if number then return false otherwise true
*/