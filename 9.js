const data = ['Peter', 'anil', 'sam'];
const [y] = data;
const [a, b, c] = data;
console.log(y)
console.log(a, b, c);

/**
 1> y will hold value of element present at index 0 of data array
 2> its a special type of destructuring, we can destructure and access any element 
 by this method
 */